# Run npm scripts through bash on every platform so the quoted globs in the
# test scripts (node --test 'test/**/*.test.ts') are stripped by a POSIX shell.
# On Windows, npm's default cmd.exe passes the single quotes through literally,
# which makes node --test match zero files. Git Bash is on PATH here and on CI.
script-shell=bash
